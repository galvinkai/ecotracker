{
  "name": "ecotracker-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash ./build.sh || cd ecotracker && npm install && npm run build",
    "start": "cd ecotracker && npm start",
    "dev": "cd ecotracker && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
